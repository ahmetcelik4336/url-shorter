{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Backend (Beego)",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/api/main.go",
            "preLaunchTask": "api"
        },
        {
            "name": "Frontend (Client)",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/client_/main.go",
            "preLaunchTask": "client_"
        }
    ],
    "compounds": [
        {
            "name": "Full Stack Başlat",
            "configurations": ["Backend (Beego)", "Frontend (Client)"],
            "stopAll": true
        }
    ]
}